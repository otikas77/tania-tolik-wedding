import { BLOCKS_ID } from '../../../constants/common';
import './hero.styles.scss';

export const Hero = () => {
    return <section id={BLOCKS_ID.heroSection}>
        <h1>Anatolii & Tetiana</h1>
    </section>
}